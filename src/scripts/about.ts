// About page scripts.
// The "Way We See Brands" section now uses native full-section slides
// (no sticky scroll-story) so there's nothing JS-driven to wire here.
// File kept so existing imports continue to resolve.

export {};
